name: Gmeek Blog Deploy
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build & Deploy Gmeek
        uses: Meekdai/GmeekAction@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
